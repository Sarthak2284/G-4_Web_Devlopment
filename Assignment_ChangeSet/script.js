const add2numbers = (a,b)=>{
    return a + b;
}

const subtract2numbers = (a,b)=>{
    return a - b;
}

const multiply2numbers = (a,b)=>{
    return a * b;
}

module.exports = {
    add2numbers,
    subtract2numbers,
    multiply2numbers
};